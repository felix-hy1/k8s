apiVersion: v1
kind: Pod
metadata:
  name: test-pod
  namespace: pod-lab
spec:
  containers:
    - name: app
      image: nginx:1.27
      livenessProbe:
        httpGet : {path: /, port: 80}
        initialDelaySeconds: 5
        periodSeconds: 10
        failureThreshold: 3
