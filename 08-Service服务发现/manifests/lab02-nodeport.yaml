# 实验 2:NodePort(每个节点开一个高位端口)
apiVersion: v1
kind: Service
metadata:
  name: web-np
  namespace: svc-lab
spec:
  type: NodePort
  selector:
    app: web
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080              # 不写则自动分配 30000-32767
